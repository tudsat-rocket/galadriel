[package]
name = "galadriel"
version = "0.1.0"
edition = "2021"

[dependencies]
nalgebra = "0.32.3"

[dev-dependencies]
criterion = { version = "0.4", features = ["html_reports"] }

[[bench]]
name = "bench"
harness = false
